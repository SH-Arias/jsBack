const pg = require('pg');

const connectionData ={
    user: 'postgres',
    host: 'base',
    database: 'proyectoBases',
    password: 'stemen',
    port: 5432,
}
const conexion = new pg.Pool(connectionData);